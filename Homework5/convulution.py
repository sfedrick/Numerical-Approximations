import numpy as np
import matplotlib.pyplot as plt
from pandas.core.common import flatten
def y1(x):
    f=np.sin(3*x)+0.1*np.sin(16*x)
    return f
def y2(x):
    f=np.cos(3*x)+0.1*np.cos*(16*x)
    return f
